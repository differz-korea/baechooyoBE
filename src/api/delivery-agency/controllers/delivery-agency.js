'use strict';

/**
 * delivery-agency controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::delivery-agency.delivery-agency');
