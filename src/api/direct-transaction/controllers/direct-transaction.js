'use strict';

/**
 * direct-transaction controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::direct-transaction.direct-transaction');
